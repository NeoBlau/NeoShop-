# assets/ and vendor/ are committed on purpose: the folder is a complete,
# offline-ready copy of the game (textures + three.js), so downloading it
# from GitHub is enough to play. `node tools/fetch-assets.mjs` re-creates them.
node_modules/

# The monorepo ignores every build/ directory; three.js ships its modules in one.
!vendor/three/build/
